# Context Plugin Binary

`yunxi-context.rs` starts only the isolated context plugin protocol loop. It is
available for external packaging even though the default CLI can launch the
same library through its private child-process mode.
