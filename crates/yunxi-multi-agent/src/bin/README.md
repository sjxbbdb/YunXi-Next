# Multi-agent Binary

`yunxi-multi-agent.rs` is the thin standalone entry point for the isolated
coordinator. Runtime behavior remains in the library so the bundled CLI child
mode and process tests use the same implementation.
