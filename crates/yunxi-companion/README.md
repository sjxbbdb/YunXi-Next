# yunxi-companion

Process-isolated `companion.decide@1` provider. It classifies bounded emotional
signals and returns deterministic tone/follow-up guidance without calling a
model or executing tools.

The host decides whether this plugin is enabled. The plugin cannot initiate a
message, approve an action, or access storage.
