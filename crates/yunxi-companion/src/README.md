# Source Map

- `lib.rs` exposes the companion policy facade and plugin entry point.
- `decision.rs` contains deterministic emotion and tone policy.
- `plugin.rs` dispatches the versioned capability call.
- `bin/` contains the standalone plugin executable.
