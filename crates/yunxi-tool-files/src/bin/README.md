# Binary

`yunxi-tool-files` is the thin standalone entry point for the read-only file
tools plugin. The CLI normally launches the reusable library through the
unified `yunxi-next` executable's private child mode.
