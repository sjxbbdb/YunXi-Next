# Binaries

- `yunxi-tool-mcp` is the standalone `tool.mcp@1` plugin entry point.
- `yunxi-mcp-fixture` is a small deterministic MCP Server used by integration
  tests for normal, malformed, and crash boundaries.
